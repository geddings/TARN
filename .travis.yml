language: java

sudo: enabled

dist: trusty

install:
  - ./.travis/install_dependencies
  - sudo ./.travis/install_ovs
  - sudo ./.travis/start_ovs
  - ./.travis/install_mininet
  - ./.travis/install_mininext

script: sudo python ./mininet/topologies/simplenobgptopo.py

notifications:
  email: false